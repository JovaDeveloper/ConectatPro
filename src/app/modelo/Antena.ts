export interface Antena
{
    id ? : string;
    serial ? : string,
    mac ? : string;
    status ? : string;
    fecha ? : string;
    created_by ? : string;
    created_at ? : string;
    updated_by ? : string;
    updated_at ? : string;
}