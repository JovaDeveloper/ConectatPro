export interface Vendedor
{
    id ? : string;
    nombre_completo ? : string;
    telefono ? : string;
    categoria ? : string;
    direccion ? : string;
    tipo_vendedor ? : string;
    created_by ? : string;
    created_at ? : string;
    updated_by ? : string;
    updated_at ? : string;
}