export interface Instalador
{
    id ? : string;
    nombre_completo ? : string;
    tipo_trabajador ? : string;
    telefono ? : string;
    direccion ? : string;
    created_by ? : string;
    created_at ? : string;
    updated_by ? : string;
    updated_at ? : string;
}
