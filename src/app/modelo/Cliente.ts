export interface Cliente
{
    id ? : string;
    n_contrato ? : string;
    nombre_completo ? : string;
    telefono ? : string;
    direccion ? : string;
    paquete ? : string;
    sector ? : string;
    estacion ? : string;
    status ? :  string;
    fecha_contratacion ? : string;
    created_by ? : string;
    created_at ? : string;
    updated_by ? : string;
    updated_at ? : string;
    id_instalacion ? : string;
    id_vendedor ? : string;
    
}